kind: Service
apiVersion: v1
metadata:
  name: mysql-service
  namespace: mysql

spec:
  selector:
      app: mysql
  clusterIP: None
  ports:
  - name: mysql
    protocol: TCP
    port: 3306
    targetPort: 3306 
